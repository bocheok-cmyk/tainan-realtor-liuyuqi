# 複製這個檔案，改名成 .env.local，再填入你自己的金鑰。
# .env.local 不會被上傳/進版控，金鑰只留在你自己的電腦上。

# 選填：沒有這把金鑰，「查詢附近設施」按鈕會提示你手動輸入，其他功能不受影響。
# 到 https://console.cloud.google.com/ 開一個專案，啟用 Places API (New) 和 Geocoding API，
# 建立 API 金鑰，貼在下面。記得到金鑰設定裡限制用量/加上預算警示。
GOOGLE_MAPS_API_KEY=

# 選填：沒有這把金鑰，「產生建議文案」按鈕會用內建規則制草稿（免費、離線）。
# 有填的話會改用 Claude API 生成更自然的文案。
ANTHROPIC_API_KEY=

# 選填：沒有這把金鑰，謄本「截圖辨識」按鈕不能用，PDF上傳/手動輸入不受影響。
# 到 https://aistudio.google.com/apikey 申請（用付費層，不是Gemini Advanced訂閱帳號）。
# 每天全站共用有次數上限，見 src/app/api/parse-deed-vision/route.ts 的 DAILY_LIMIT。
GEMINI_API_KEY=

# 必填（正式上線用）：內部登入 + 每日AI額度計數都存在這裡。
# 到 https://supabase.com/ 開一個新專案（獨立於其他專案），Settings > API 複製這兩個值，
# 並在 Supabase 的 SQL Editor 貼上執行 supabase/migrations/0001_ai_usage_daily.sql。
# 本機開發沒填也能跑，只是額度計數不會真的擋人（見 src/lib/rate-limit.ts）。
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 只有建立登入帳號的一次性腳本（scripts/create-admin-user.mjs）需要，不要放進正式環境變數，
# 用完可以從這裡刪掉。在 Supabase Settings > API 的 service_role 那組。
SUPABASE_SERVICE_ROLE_KEY=
